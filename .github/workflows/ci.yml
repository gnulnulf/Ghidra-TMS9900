name: ci
on: [push, pull_request, workflow_dispatch]

jobs:
  build_and_test:
    name: Build + test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ghidra:
          - "12.0.4"
          - "12.0.3"
          - "11.4.3"
          - "11.4.2"
          - "11.3.2"
          - "11.2.1"
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'
      - name: Install Ghidra ${{ matrix.ghidra }}
        uses: antoniovazquezblanco/setup-ghidra@v2.0.19
        with:
          auth_token: ${{ secrets.GITHUB_TOKEN }}
          version: ${{ matrix.ghidra }}
      - name: gradlew
        run: gradle wrapper
      - name: Build
        run: ./gradlew test buildExtension -PGHIDRA_INSTALL_DIR=${{ env.GHIDRA_INSTALL_DIR }}
      - uses: actions/upload-artifact@v7
        with:
          name: Ghidra-TMS9900_Ghidra_${{ matrix.ghidra }}
          path: dist/*_Ghidra-TMS9900.zip
          if-no-files-found: 'error'
  release:
    if: github.ref_type == 'tag'
    name: Upload release artifacts
    runs-on: ubuntu-latest
    needs: build_and_test
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v8
        with:
          pattern: Ghidra-TMS9900_Ghidra_*
      - name: Upload distribution
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" Ghidra-TMS9900_Ghidra_*/*_Ghidra-TMS9900.zip